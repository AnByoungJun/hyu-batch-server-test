# prod 환경변수 예시 → 복사해서 `.env.prod` 로 작성 (실제 파일은 git 미포함)
# DB_USERNAME / DB_PASSWORD 는 필수 (미주입 시 기동 실패)
DB_URL=jdbc:postgresql://172.25.0.21:5432/bim_gis
DB_USERNAME=
DB_PASSWORD=
DB_POOL_MAX=5
SERVER_PORT=8082
PRODUCTIVITY_API_URL=
