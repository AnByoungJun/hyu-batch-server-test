# base 정의 (환경 무관 공통). 단독으로 쓰지 않고 dev/prod override 와 함께 사용한다.
#   dev :  docker compose -f docker-compose.yml -f docker-compose.dev.yml  up -d
#   prod:  docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# 이미지 주소(image)와 비밀(env_file)은 각 override 에서 지정한다.
services:
  hyu-batch:
    container_name: hyu-batch-server-test
    ports:
      - "8083:8082"
    restart: unless-stopped
    # 컨테이너 내부(8082, context-path /batch) actuator health 로 상태 감시
    #   start_period 동안(부팅 중)엔 실패해도 unhealthy 로 안 봄
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/batch/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
